// import {
//
// } from "../actions/graph";

const initialState = {};

const visitReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default visitReducer;
